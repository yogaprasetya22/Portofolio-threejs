rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /posts/{userId}/{document=**} {
      allow read, write: if request.auth.uid != null;
     allow read, create, delete, write: if request.auth.uid == userId;
    }
  }
}

