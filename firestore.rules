rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /posts/{document=**} {
      allow read, write: if request.auth.uid != null;
     allow read, create, delete, write: if true
    }
  }
}